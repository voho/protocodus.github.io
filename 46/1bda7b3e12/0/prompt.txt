A session-scoped Stop hook is now active with condition: "create a new fun (game) called sommer. it will be similar principle as alpen with infinite generated terrain, but it will be a summer game, where you will ride a bmx bike (first person view) downhill. get inspired by images. terrain: pine forest, mixed forest, narrow rocky roads, meadows, bike track, dirty roads... make the terrain rich and download public textures as needed". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.